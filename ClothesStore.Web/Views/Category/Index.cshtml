@model List<ClothesStore_Entities.Category>
@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>
<h3>Helllo ww</h3>
@foreach(var cat in Model)
            {
                <div class="card border border-primary my-2" style="width: 18rem;">
                    
                    <div class="card-body ">
                        <h2>@cat.Name</h2>
                        <h3>@cat.Description</h3>
                    </div>
                </div>
                
            }

